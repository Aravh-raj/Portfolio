# Aravh Raj — Portfolio Environment Configuration
# This portfolio is a client-side reactive web application and does NOT require
# any secret keys, API tokens, or external database credentials to build or deploy.

# Optional custom public variables (if integrated with analytics in future):
# VITE_PUBLIC_SITE_URL=https://aravhraj.vercel.app
